version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'weekly'
      day: 'monday'
      time: '09:00'
      timezone: 'America/Los_Angeles'
    open-pull-requests-limit: 5
    commit-message:
      # prefix 'chore' + include 'scope' yields chore(deps): / chore(deps-dev):.
      # prefix 'chore(deps)' would emit chore(deps)(deps-dev): double scopes that
      # break release-please and the semantic PR-title check.
      prefix: 'chore'
      include: 'scope'
    labels:
      - 'dependencies'
      - 'npm'
    reviewers:
      - 'pdugan20'
    assignees:
      - 'pdugan20'
    # TypeScript 7 does not expose the programmatic API required by the current
    # typescript-eslint toolchain. Revisit only with a proven TS7/dual-toolchain
    # compatibility envelope.
    ignore:
      - dependency-name: 'typescript'
        versions:
          - '>=7'
    groups:
      development-dependencies:
        patterns:
          - '@types/*'
          - 'eslint*'
          - 'prettier*'
          - '@typescript-eslint/*'
          - 'webpack*'
          - 'ts-*'
        update-types:
          - 'minor'
          - 'patch'
      production-dependencies:
        patterns:
          - '*'
        exclude-patterns:
          - '@types/*'
          - 'eslint*'
          - 'prettier*'
          - '@typescript-eslint/*'
          - 'webpack*'
          - 'ts-*'
        update-types:
          - 'minor'
          - 'patch'

  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'weekly'
      day: 'monday'
      time: '09:00'
      timezone: 'America/Los_Angeles'
    open-pull-requests-limit: 3
    commit-message:
      prefix: 'chore(ci)'
    labels:
      - 'dependencies'
      - 'github-actions'
    reviewers:
      - 'pdugan20'
    assignees:
      - 'pdugan20'
